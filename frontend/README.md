# Frontend

Frontend implementation is pending the approved design system.

No screens, components, routes, Storybook, design tokens, or frontend build tooling are implemented in this task. Future frontend work must follow `SPEC.md`, consume the backend API, use the approved design system, and keep all filtering/reporting on local backend data rather than querying Bitrix directly.
